<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-3">
    <%= link_to 'Nouvelle année budgetaire', new_season_path, class: 'btn btn-success btn-lg' %>
    <h5>Années budgetaires précédentes</h5>
    <ul>
    <% @seasons.each do |season| %>
      <li><%= link_to season.year, season_path(season) %></li>
    <% end %>
    </ul>
  </div>
  <div class="col-md-9">
    <h1>Années budgetaires actuelle</h1>

  </div>
</div>
