<% if logged_in? && !has_establishment? %>
<div class="alert alert-warning" role="alert">
  <h4 class="alert-heading">Attention!</h4>
  <div>Aucun etablissement n'est associé à votre compte.
  <b><%= link_to 'Créer un établissement ici', new_establishment_path, class: 'text-warning' %></b>.
  </div>
</div>
<% end %>
