<h1 class="text-center">
  <span class="badge badge-secondary"><%= @account.pcop_category.id %></span>
  <%= @account.pcop_category.name %> -
  <small>Ajouter un compte</small>
</h1>

<%= form_for @account, url: pcop_category_accounts_path(@account.pcop_category) do |f| %>

  <%= render partial: 'shared/error_messages', locals: {model: @account} %>
  <div class="form-group">
    <%= f.label :id %>
    <%= f.text_field :id, class: 'form-control form-control-sm' %>
  </div>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control form-control-sm' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: 'form-control form-control-sm' %>
  </div>

  <%= f.submit 'Créer le compte', class: 'btn btn-primary btn-sm' %>
<% end %>
